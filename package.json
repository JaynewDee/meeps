{
  "name": "meeps",
  "version": "1.0.0",
  "description": "serving only the chirpiest meeps",
  "main": "./server/server.js",
  "author": "Joshua Newell Diehl",
  "license": "MIT",
  "engines": {
    "node": ">=16 <17"
  },
  "scripts": {
    "start": "node server/server.js",
    "build": "cd client && npm run build",
    "install": "cd client && npm install && cd ../server && npm install"
  },
  "dependencies": {
    "typescript": "^4.9.3"
  }
}
